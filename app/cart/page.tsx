import CartComponent from "../../src/components/CartComponent/CartComponent";

const Cart = (props: any) => {
  return (
    <div className="flex justify-center">
        <CartComponent />
    </div>
  );
};

export default Cart;
